import { JewelfilterPipe } from './jewelfilter.pipe';

describe('JewelfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JewelfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
